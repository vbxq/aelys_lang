[package]
name = "aelys-opt"
version.workspace = true
edition = "2024"

[dependencies]
aelys-common = { path = "../common" }
aelys-syntax = { path = "../syntax" }
aelys-sema = { path = "../sema" }
aelys-air = { path = "../air" }

[lib]
# enabled for the phase-token structural guard on Optimizer::optimize
doctest = true