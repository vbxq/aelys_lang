fn main() -> i64 {
    let a = 10
    let b = 20
    let sum = a + b
    let product = a * b
    let diff = b - a
    let quot = b / a
    let result = sum + product + diff + quot
    println(result)
    println(result)
    println(result)
}